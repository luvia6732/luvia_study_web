<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
      "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
      
<mapper namespace="TEST">

	<select id="connectionTest" resultType="String">
		SELECT
			DATE_FORMAT (NOW(), '%y-%m-%d %H:%i:%s')
		FROM
			DUAL;
	</select>
</mapper>